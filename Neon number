#include <stdio.h>

int main() {
  int n,i,re,sum=0,sq;
  scanf("%d",&n);
  sq=n*n;
  while(sq!=0){
      re=sq%10;
      sum+=re;
      sq/=10;
  }
  if(sum==n){
      printf("neon number");
  }
  else{
      printf("not neon number");
  }
  return 0;
}
