#include <stdio.h>

int main()
{
    int n, rem, max=0;

    printf("Enter number: ");
    scanf("%d",&n);

    while(n!=0)
    {
        rem=n%10;
        if(rem>max)
            max=rem;
        n/=10;
    }

    printf("Largest Digit = %d",max);

    return 0;
}
