#include <stdio.h>

int main()
{
    int n, rem, min=9;

    printf("Enter number: ");
    scanf("%d",&n);

    while(n!=0)
    {
        rem=n%10;
        if(rem<min)
            min=rem;
        n/=10;
    }

    printf("Smallest Digit = %d",min);

    return 0;
}
